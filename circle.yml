machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build --rm=false -t electrified/notes .

# test:
#   override:
#     - docker run -d -p 9200:9200 electrified/notes

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push electrified/notes
